import React from 'react';
import ReactDOM from 'react-dom';
//import {BrowserRouter as Router, Route, Switch} from "react-router-dom";

/*
ReactDOM.render(
    (<Router>
        <Switch>
        <Route exact={true} path="/" component={HomePage}/>
        <Route path="/submit" component={App}/>
        <Route path="/history" component={LogsPage}/>
        <Route path="/logs" component={EnhancedTable}/>
        </Switch>
    </Router>), document.getElementById('react')
);
*/
ReactDOM.render(
    (<span>Hello world!</span>), document.getElementById('react')
);